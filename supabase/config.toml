project_id = "aeskolar"

[auth]
# IMPORTANT:
# If you are using hosted Supabase (not Supabase CLI locally),
# you must also set these in the Supabase Dashboard:
# Auth → URL Configuration → Site URL / Redirect URLs
# Keep in sync with vite.config.js server.port (5176)
site_url = "http://localhost:5176"
additional_redirect_urls = [
  "http://localhost:5176/**",
  "http://localhost:5173/**",
]

